version: 2.1

jobs:
  journeys:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_journeys.py -v"}]
  privacy:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_privacy.py -v"}]
  release:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_release.py -v"}]
  runtime_journeys:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_runtime_journeys.py -v"}]
  security:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_security.py -v"}]
  store:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_store.py -v"}]
  studio:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_studio.py -v"}]
  transport:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_transport.py -v"}]
  capability:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_capability_qa.py -v"}]
  performance:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_performance_qa.py -v"}]
  native:
    docker: [{image: cimg/python:3.12.11}]
    steps: [checkout, {run: "python tests/test_native_qa.py -v"}]

workflows:
  diagnose:
    jobs:
      - journeys
      - privacy
      - release
      - runtime_journeys
      - security
      - store
      - studio
      - transport
      - capability
      - performance
      - native
