version: 2.1

jobs:
  visual_full:
    machine: {image: ubuntu-2404:current}
    resource_class: large
    steps:
      - checkout
      - run:
          no_output_timeout: 30m
          command: python3 studio/smoke_probe.py visual

workflows:
  validate-visual:
    jobs:
      - visual_full
